# 공통 프롬프트 탭 개발 인수인계

- 작성일: 2026-07-14 (KST)
- 대상 저장소: `D:\개발관련\ppt_prompt`
- 대상 기능: PromptDeck의 `공통 프롬프트` 탭
- 원 개발정의서: 외부 로컬 문서(저장소 미포함)
- 현재 실행 주소: `http://127.0.0.1:8765/index.html`

## 1. 인수인계 요약

`공통 프롬프트` 탭은 슬라이드별 실제 콘텐츠를 작성하는 곳이 아니라, 발표자료 전체에 공통 적용할 **슬라이드 규격과 디자인 시스템만 생성하는 도구**다. 생성 결과는 `슬라이드 분리기`의 공통 프롬프트 입력으로 전달된다.

현재 신규 탭, 13개 아코디언 설정 구조, 실시간 요약, 충돌 검사, 프롬프트 생성, JSON 저장/불러오기, 슬라이드 분리기 전달까지 구현되어 있다.

최신 UX 원칙은 다음과 같다.

1. 데스크톱은 왼쪽 설정 아코디언과 오른쪽 고정 요약 패널로 구성한다.
2. 프로젝트명·제작모델·출력언어·출력형식으로 이루어진 최상단 입력 바는 두지 않는다.
3. 제작모델·출력언어·출력형식은 `출력 설정` 대화상자에서 관리한다.
4. 아코디언은 한 번에 최대 한 항목만 열린다.
5. 각 설정 항목은 `설정 / 비설정`으로 전환할 수 있다.
6. 비설정 항목의 값은 삭제하지 않고 보존하지만, 프롬프트 생성과 충돌 검사에서는 제외한다.
7. 모든 설정 항목을 비설정으로 둘 수 있다. 이 경우에도 콘텐츠 임의 생성 방지를 위한 최소 범위 계약은 유지한다.

## 2. 개발 목적과 방향

### 2.1 핵심 목적

- 슬라이드 전체의 시각적 일관성을 정의하는 공통 디자인 프롬프트를 만든다.
- 슬라이드별 제목, 본문, 수치, 이미지 주제, 표·차트 데이터는 공통 프롬프트가 임의로 만들지 않는다.
- 공통 프롬프트와 개별 슬라이드 명세가 충돌할 때 사실·문구·수치·의미는 개별 명세를 우선한다.
- GPT Image, Gemini, 공통형 이미지 생성 모델에서 이해하기 쉬운 구조화된 자연어 프롬프트를 만든다.

### 2.2 제품 설계 원칙

- 선택 우선: 직접 문장 작성보다 프리셋, 선택지, 체크박스와 보조 입력을 우선한다.
- 단계적 노출: 필요한 세부 설정만 조건부로 보여준다.
- 비설정 허용: 값을 정하지 않는 것도 유효한 사용자 결정으로 취급한다.
- 값 보존: 비설정 전환 시 기존 입력값을 초기화하지 않는다.
- 충돌 방지: 잘못된 조합을 검사하되, 비설정 항목은 검사 대상에서 제외한다.
- 콘텐츠 분리: 실제 슬라이드 콘텐츠를 추론하거나 추가하지 않는다.
- 반복 사용성: 장식보다 빠른 탐색, 수정, 복사와 분리기 전달을 우선한다.

## 3. 현재 화면 및 사용자 흐름

### 3.1 탭 진입

- 상단 탭: `공통 프롬프트`
- 탭 버튼: `#tabBtnCommonPrompt`
- 탭 패널: `#paneCommonPrompt`
- 앱 루트: `#commonPromptApp`

### 3.2 설정 순서

1. 프로젝트 기본정보
2. 슬라이드 규격
3. 디자인 방향
4. 색상 시스템
5. 배경 시스템
6. 헤더 시스템
7. 푸터 시스템
8. 타이포그래피
9. 카드·표·차트
10. 이미지·아이콘
11. 가독성·품질
12. 금지 규칙
13. 검토 및 생성

1~12번은 `설정 / 비설정` 전환이 가능하다. 13번 `검토 및 생성`은 동작 항목이므로 비설정 대상이 아니다.

### 3.3 아코디언 동작

- 설정 제목을 선택하면 해당 항목만 열린다.
- 다른 설정을 선택하면 기존에 열려 있던 항목은 자동으로 닫힌다.
- 열린 항목을 다시 선택하면 모든 항목이 닫힌 상태가 된다.
- 상단에는 `모두 접기`만 제공한다.
- 과거에 있던 `모두 펼치기` 기능은 최신 요구사항과 충돌하므로 제거했다.
- 상단 진행률은 완료 단계가 아니라 `사용 중인 설정 수 / 12`를 표시한다.

### 3.4 비설정 동작

- 각 아코디언 제목 오른쪽의 필 버튼으로 `설정 / 비설정`을 전환한다.
- 비설정 상태에서 항목을 열면 다음 안내를 표시한다.
  - 저장된 입력값은 유지된다.
  - 생성 프롬프트와 충돌 검사에서는 제외된다.
  - `설정 사용하기` 버튼으로 다시 활성화할 수 있다.
- 비설정 항목의 요약 문구는 `프롬프트에서 제외됨`으로 표시한다.
- 브라우저의 저장된 상태에 따라 사용 중인 항목 수가 기본값과 다르게 보일 수 있다.

## 4. 파일 구조와 역할

### 4.1 핵심 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 공통 프롬프트 탭 버튼, 탭 패널, CSS/JS 로딩 |
| `src/tabs.js` | 탭 등록과 공통 프롬프트 탭의 상단 퀵 액션 연결 |
| `src/common-prompt.js` | 상태, 렌더링, 검증, 프롬프트 조립, 저장, 분리기 전달 |
| `styles/common-prompt.css` | 데스크톱·모바일 레이아웃, 아코디언, 요약, 대화상자 스타일 |
| `src/generator.js` | 슬라이드 분리기 본체 |
| `design-qa.md` | 과거 시점의 공통 프롬프트 시각 QA 기록 |

### 4.2 탭 연결 필수 요소

다음 요소는 한 세트로 유지해야 한다.

- `index.html`의 `#tabBtnCommonPrompt`
- `index.html`의 `#paneCommonPrompt`
- `styles/common-prompt.css` 로딩
- `src/common-prompt.js` 로딩
- `src/tabs.js`의 `tabs.commonPrompt`
- `src/tabs.js`의 `actionSets.commonPrompt`

상단 퀵 액션은 숨겨진 프록시 버튼을 통해 연결된다.

- `#commonPromptGenerateBtn`
- `#commonPromptSendGeneratorBtn`
- `#commonPromptCopyBtn`
- `#commonPromptResetBtn`

## 5. 상태 모델

### 5.1 저장 위치

- localStorage 키: `promptdeck.commonPromptBuilder.v1`
- 스키마 버전: `1.0`
- 새 필드는 `merge(DEFAULT_STATE, parsed)` 방식으로 과거 저장 데이터에 병합된다.

### 5.2 주요 상태

- `activeStep`: 마지막으로 선택한 아코디언 번호
- `sectionEnabled`: 1~12번 설정 항목의 사용 여부
- `project`: 결과물 유형, 사용 목적, 생성 모델, 언어, 출력 형식
- `canvas`: 화면비, 해상도, 방향, 출력 기준, 외부 여백, 안전영역
- `visualDirection`: 프리셋, 시각적 강도, 디자인 키워드
- `colors`: 역할별 색상과 사진 색상 정책
- `background`: 배경 유형과 사진 보호 규칙
- `header`, `footer`: 공통 헤더·푸터 규칙
- `typography`: 글꼴 성격, 크기, 간격, 원문 보호
- `components`: 카드·표·차트 스타일
- `imagery`: 이미지와 아이콘 정책
- `quality`: 대비, 원문·수치 보존, 한글 렌더링
- `constraints`: 금지 규칙
- `output`: 마지막 생성 프롬프트와 생성 시각

`sectionEnabled`의 코드 기본값은 12개 항목 모두 `true`다. 다만 실제 브라우저에서는 이전에 저장한 사용자의 비설정 상태가 우선 적용된다.

### 5.3 비설정 판단

`isSectionEnabled(id)`가 공통 판단 함수다.

- `review`는 항상 활성 상태다.
- `sectionEnabled[id] === false`일 때만 비설정으로 판단한다.
- 필드 값 자체는 수정하거나 삭제하지 않는다.

## 6. 슬라이드 규격 구현 현황

`2. 슬라이드 규격`에는 입력 편의 기능이 추가되어 있다.

### 6.1 빠른 규격

- 16:9 FHD: 1920×1080
- 16:9 QHD: 2560×1440
- 16:9 4K: 3840×2160
- 4:3 표준: 1600×1200
- A4 가로: 3508×2480
- A4 세로: 2480×3508

동일한 규격을 다시 선택하면 중복 적용하지 않고 안내 토스트를 표시한다.

### 6.2 화면비와 해상도

- 화면비, 너비, 높이, 방향별 도움말 제공
- 사용자 지정 화면비 지원
- `화면비에 맞춰 해상도 자동 계산` 지원
- 너비 또는 높이를 바꾸면 반대 값을 자동 계산
- 화면비 일치/불일치를 즉시 표시
- 불일치 시 `자동 보정` 제공

### 6.3 여백과 안전영역

- 외부 여백: 좁게, 표준, 넓게, 직접 입력
- 안전영역: 자동 6%, 4%, 6%, 8%, 10%
- 상·하·좌·우 같은 값 연동 입력
- 동일 안전영역을 다시 선택하면 중복 안내

## 7. 프롬프트 생성 규칙

### 7.1 기본 조립 원칙

`buildPrompt()`가 최종 문자열을 만든다.

- 출력 언어: 국문 또는 영문
- 출력 형식: 상세형, 표준형, 압축형, Deck Style Lock
- 제작 모델: GPT Image, Gemini, 공통형
- 활성화된 설정 항목만 해당 프롬프트 섹션을 추가한다.
- 비설정 항목은 `없음`이라는 명령도 만들지 않고 완전히 생략한다.

예를 들어 `디자인 방향`이 비설정이면 다음 내용이 모두 빠져야 한다.

- `### 시각 방향` 섹션
- 디자인 프리셋명
- 시각적 강도
- 디자인 키워드

### 7.2 헤더·푸터 예외

헤더와 푸터는 하나의 프롬프트 섹션으로 조립되지만 활성 상태는 각각 독립적이다.

- 헤더만 설정: 헤더 규칙만 포함
- 푸터만 설정: 푸터 규칙만 포함
- 둘 다 비설정: `헤더 및 푸터` 섹션 전체 생략
- 항목이 비설정이면 `헤더를 사용하지 않는다` 같은 부정 명령도 출력하지 않는다.

### 7.3 항상 유지하는 최소 안전 계약

모든 사용자 설정을 비설정으로 바꿔도 아래 원칙은 유지한다.

- 실제 콘텐츠와 공통 디자인 규칙의 분리
- 개별 슬라이드 명세를 콘텐츠와 구성의 유일한 출처로 사용
- 명세에 없는 사실·문구·수치·이미지 주제를 임의 생성하지 않음
- 개별 명세와 디자인 규칙 충돌 시 사실과 의미 보존

이 부분은 `REQUIRED_SCOPE`와 개별 명세 우선 섹션으로 구현되어 있으며, 일반 설정 토글 대상이 아니다.

### 7.4 압축형

압축형도 항목별 활성 상태를 반영한다. 시각 방향·색상, 컴포넌트·이미지, 품질·금지 규칙처럼 과거에 묶여 있던 섹션도 현재는 각각 활성 여부에 따라 독립적으로 포함된다.

### 7.5 Deck Style Lock

Deck Style Lock은 출력 계약 섹션을 제외하는 방식이다. 프로젝트 기본정보가 비설정인 경우에도 범위 및 콘텐츠 분리 섹션이 잘못 제거되지 않도록 제목 기준으로 필터링한다.

## 8. 충돌 검사

`validateState()`는 활성화된 설정만 검사한다.

- 캔버스 비율 오류: 캔버스 설정이 활성일 때만 검사
- 색상 대비: 색상 설정이 활성일 때만 검사
- 헤더·푸터 페이지 번호 중복: 두 항목이 모두 활성일 때만 검사
- 팔레트 전용 규칙과 사진 자연색 충돌: 색상과 금지 규칙이 모두 활성일 때만 검사
- 사진 배경과 이미지 위 텍스트 제한: 배경과 이미지 설정이 모두 활성일 때만 검사
- 디자인 키워드 충돌: 디자인 방향이 활성일 때만 검사
- 원문·수치 보존 권장: 가독성·품질이 활성일 때만 검사

오류가 있으면 프롬프트 생성과 분리기 전달 버튼을 비활성화한다. 자동 수정 가능한 항목은 `권장안 적용`을 제공한다.

## 9. 슬라이드 분리기 연계

`sendToGenerator()`에서 최종 프롬프트를 생성한 뒤 다음 순서로 전달한다.

1. `window.PromptDeckSlidePromptGenerator.setCommonPrompt()`가 있으면 해당 API 사용
2. API가 없으면 `#genCommonPrompt` textarea에 직접 입력
3. `#tabBtnGenerator`를 선택해 슬라이드 분리기 탭으로 이동

전달 메타데이터는 다음과 같다.

- `text`: 최종 공통 프롬프트
- `lang`: 선택한 출력 언어
- `source`: `common-prompt-builder`

## 10. 저장·복원과 편집 이력

- 입력 변경은 localStorage에 자동 저장한다.
- JSON 저장 시 마지막 생성 결과는 제거하고 설정만 내보낸다.
- JSON 불러오기 시 기본 상태와 병합한다.
- 실행 취소·다시 실행 이력은 메모리에서 최대 50개를 유지한다.
- 비설정 전환도 실행 취소 이력에 포함한다.
- 새 프로젝트 초기화 시 기본 상태와 기본 아코디언 상태로 복원한다.

## 11. 완료된 검증

### 11.1 기능 검증

- 한 아코디언을 열면 기존 아코디언이 자동으로 닫힘
- 열린 항목은 항상 0개 또는 1개
- 비설정 전환 후 입력값 보존 안내 표시
- 비설정 디자인 방향이 표준형 생성 프롬프트에서 완전히 제외됨
- 비설정 금지 규칙이 생성 프롬프트에서 제외됨
- 사용 중인 캔버스 섹션은 계속 포함됨
- 비설정 항목 수가 상단 진행률에 반영됨
- 슬라이드 규격의 자동 계산, 자동 보정, 안전영역 연동과 중복 안내

### 11.2 반응형 검증

- 데스크톱 레이아웃 확인
- 모바일 390×844 확인
- 모바일 가로 넘침 없음
- 모바일에서도 설정/비설정 버튼 최소 폭 유지
- 모바일 고정 하단 액션바 유지

### 11.3 실행한 검사

```powershell
node --check src\common-prompt.js
node --check src\tabs.js
git diff --check -- index.html src\tabs.js src\common-prompt.js styles\common-prompt.css
node scripts\smoke-test.mjs
Select-String -Path index.html,src\common-prompt.js,src\tabs.js,styles\common-prompt.css -Encoding utf8 -Pattern '�'
```

마지막 검사에서 JavaScript 문법, diff 공백, UTF-8/BOM, 스모크 테스트가 통과했다.

## 12. 현재 알려진 문제와 후속 과제

### 12.1 저장소 기존 전역 오류

브라우저 콘솔에서 다음 기존 오류가 반복된다.

```text
ReferenceError: state is not defined
src/promptdeck-api-globals.js:461
```

- 이번 공통 프롬프트 구현 파일에서 발생한 오류는 아니다.
- 공통 프롬프트 생성, 아코디언, 출력 설정과 분리기 전달 검증에는 영향을 주지 않았다.
- 수정하려면 `app.js` 상태와 `src/promptdeck-api-globals.js`의 전역 노출 계약을 별도 범위에서 확인해야 한다.
- 공통 프롬프트 작업을 이유로 임의 수정하지 않는다.

### 12.2 미리보기의 비설정 반영 수준

오른쪽 요약의 규격·색상·헤더·푸터 텍스트는 비설정을 표시한다. 그러나 중립적 슬라이드 미리보기 자체는 저장된 색상·규격 값을 계속 사용한다.

후속 개발 시 다음 중 하나를 결정할 수 있다.

- 비설정 요소를 미리보기에서 흐리게 표시
- 미리보기를 `활성 설정만 반영` 모드로 변경
- 현재처럼 저장값 참고용 중립 미리보기로 유지하고 설명을 강화

### 12.3 출력 형식 회귀 테스트

표준형에서 비설정 제외 동작을 브라우저로 검증했다. 압축형과 Deck Style Lock도 코드에 반영되어 있지만 다음 개발자가 조합별 회귀 테스트를 추가하는 것이 좋다.

권장 조합은 다음과 같다.

- 모든 항목 설정
- 디자인 방향만 비설정
- 헤더만 비설정
- 헤더·푸터 모두 비설정
- 품질만 설정
- 1~12번 모두 비설정
- 국문/영문 × 표준형/압축형/Deck Style Lock

### 12.4 분석 이벤트와 백엔드

개발정의서의 분석 이벤트, 서버 저장, 사용자 프리셋 계정 동기화는 아직 구현하지 않았다. 현재는 정적 프론트엔드와 브라우저 localStorage 기반이다.

## 13. 개발 시 유의사항

### 13.1 인코딩

- 모든 파일은 UTF-8 without BOM을 유지한다.
- 한글 파일을 PowerShell로 읽을 때 `Get-Content -Encoding utf8`을 사용한다.
- `Set-Content`나 리다이렉션으로 큰 파일 전체를 다시 쓰지 않는다.
- 수정 후 `�` 문자를 검색한다.

### 13.2 수정 방식

- `index.html`, `src/common-prompt.js`, `styles/common-prompt.css`, `src/tabs.js`는 부분 패치로 수정한다.
- 기존 파일 전체 덮어쓰기를 피한다.
- 사용자가 만든 다른 변경을 되돌리지 않는다.
- 공통 프롬프트 외 다른 탭의 변경을 함께 정리하거나 포맷하지 않는다.

### 13.3 상태 호환성

- 새 상태 필드는 반드시 `DEFAULT_STATE`에 기본값을 추가한다.
- 과거 localStorage와 JSON 설정을 깨지 않도록 `merge()` 호환성을 유지한다.
- `sectionEnabled`가 없는 과거 저장 데이터는 모든 항목 설정 상태로 열려야 한다.
- 비설정 전환 시 하위 설정값을 초기화하지 않는다.

### 13.4 프롬프트 생성

- 비설정은 `없음` 명령과 다르다.
  - 헤더 유형 `none`: 공통 헤더를 사용하지 말라는 명시적 디자인 규칙
  - 헤더 항목 `비설정`: 헤더에 대해 아무 지시도 하지 않음
- 이 두 의미를 합치지 않는다.
- 비설정 항목을 요약 문장이나 압축형 결합 섹션을 통해 다시 끌어오지 않도록 주의한다.
- 실제 제목, 본문, 수치, 출처, 이미지 주제와 차트 데이터는 생성하지 않는다.
- 최소 콘텐츠 분리 계약은 일반 설정 토글로 제거하지 않는다.

### 13.5 아코디언

- `expandedSteps`는 과거 다중 열림 구조의 `Set`을 유지하지만, 클릭 시 새 `Set([index])`으로 교체한다.
- `모두 펼치기`를 다시 추가하면 최신 요구사항과 충돌한다.
- 설정 전환 버튼을 아코디언 제목 버튼 안에 중첩하지 않는다. 현재 별도 형제 버튼으로 구성해 유효한 버튼 구조와 접근성을 유지한다.
- `aria-expanded`, `aria-controls`, `aria-pressed`, 전환 버튼의 동적 `aria-label`을 유지한다.

### 13.6 탭과 퀵 액션

- 탭을 추가·수정할 때 버튼, 패널, CSS, JS, `tabs` 등록과 `actionSets` 등록을 함께 확인한다.
- `#tabActions`는 `.app-tabs-bar` 오른쪽에 있어야 한다.
- 모바일에서는 상단 퀵 액션을 숨기고 공통 프롬프트 하단 고정 액션바를 사용한다.

### 13.7 브라우저 상태

- localStorage 때문에 코드 기본값과 실제 화면 상태가 다를 수 있다.
- 테스트 전 무조건 사용자 저장 상태를 삭제하지 않는다.
- 테스트 중 설정을 변경했다면 원래 상태로 복구한다.
- 정적 파일 변경 후 브라우저를 새로고침해야 한다.

## 14. Git 및 작업트리 주의사항

현재 작업트리는 공통 프롬프트 외 다른 기능의 변경도 많이 포함한 dirty 상태다.

특히 다음 파일은 현재 Git에서 신규 미추적 파일이다.

- `src/common-prompt.js`
- `styles/common-prompt.css`

따라서 다음 사항을 반드시 지킨다.

- 사용자의 명시적 요청 전에는 스테이징, 커밋, 푸시하지 않는다.
- 공통 프롬프트 파일이 미추적 상태라는 이유로 삭제하거나 초기화하지 않는다.
- `git reset --hard`, `git checkout --`, `git clean`을 사용하지 않는다.
- 커밋 요청을 받으면 먼저 `git status --short`와 `git diff --stat`로 공통 프롬프트 관련 범위를 분리한다.
- `tmp/`, `outputs/`, `.claude/`, `.codex-remote-attachments/`, `.playwright-mcp/`, `node_modules/`는 사용자 요청 없이 커밋하지 않는다.

## 15. QA 문서 주의사항

`design-qa.md`의 `공통 프롬프트 아코디언 레이아웃 Design QA`는 초기 아코디언 버전을 기준으로 작성되었다.

다음 내용은 현재 구현과 다르다.

- 과거: `모두 펼치기 / 모두 접기`
- 현재: 한 번에 한 항목만 열기 + `모두 접기`
- 과거: 완료 단계 기반 진행률
- 현재: 사용 중인 설정 항목 수 기반 진행률
- 현재 추가: 항목별 `설정 / 비설정`

새로운 시각 QA를 수행할 때 기존 문서를 최신 상태로 갱신하거나 별도 최신 QA 섹션을 추가해야 한다.

## 16. 다음 개발자가 권장하는 시작 순서

1. 저장소 루트의 `AGENTS.md`를 읽는다.
2. 이 인수인계 문서와 원 개발정의서를 읽는다.
3. `index.html`, `src/tabs.js`, `src/common-prompt.js`, `styles/common-prompt.css` 연결을 확인한다.
4. `http://127.0.0.1:8765/index.html`에서 `공통 프롬프트` 탭을 연다.
5. localStorage에 저장된 현재 사용자 상태를 존중한다.
6. 한 항목 열기, 비설정 전환, 프롬프트 생성과 분리기 전달을 먼저 회귀 확인한다.
7. 변경 후 문법, 인코딩, diff, 스모크 테스트와 데스크톱·모바일 브라우저 검증을 수행한다.
8. 다른 탭의 기존 변경과 전역 API 오류는 별도 범위로 취급한다.

## 17. 권장 완료 기준

후속 변경은 최소한 다음 조건을 만족해야 한다.

- 한 번에 열린 아코디언이 1개 이하
- 1~12번 설정의 독립적인 설정/비설정 전환
- 비설정 값 보존
- 비설정 항목의 검증 제외
- 비설정 항목의 표준형·압축형·Deck Style Lock 출력 제외
- 콘텐츠 분리 최소 계약 유지
- 슬라이드 분리기 전달 성공
- 데스크톱과 390px 모바일에서 가로 넘침 없음
- 키보드 접근성과 ARIA 상태 유지
- JavaScript 문법 검사 통과
- UTF-8 without BOM 유지
- 스모크 테스트 통과

## 18. 현재 상태 결론

현재 공통 프롬프트 탭은 MVP 사용 흐름을 완료한 상태다. 다음 개발의 우선순위는 새 기능을 급히 추가하는 것보다 다음 세 가지가 적합하다.

1. 비설정 조합별 출력 형식 회귀 테스트 강화
2. 비설정 상태를 오른쪽 시각 미리보기에 어떻게 표현할지 UX 결정
3. 기존 전역 API 오류를 공통 프롬프트와 분리된 작업으로 정리

배포, Git 커밋과 원격 저장소 푸시는 아직 수행하지 않았다.
